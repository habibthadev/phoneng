import { Phone, Github } from "lucide-react";

export function PhoneIcon() {
  return <Phone size={18} strokeWidth={2} fill="white" stroke="white" />;
}

export function GithubIcon() {
  return <Github size={20} strokeWidth={2} />;
}
